import { Hero } from "@/components/ui/hero-1";

const HeroSection = () => {
  return (
    <Hero 
      title="Where ideas become products and products become impact."
      subtitle="DataBuks creates custom websites, software systems, automations, and MVPs with fast delivery timelines of 1–1.5 weeks — quality, speed, and affordability in one place."
      eyebrow="Technology Services Agency"
      ctaLabel="Start Your Project"
      ctaHref="/contact"
    />
  );
};

export default HeroSection;